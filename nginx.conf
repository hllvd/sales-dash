user nginx;
worker_processes auto;

events { worker_connections 1024; }

http {
  server {
    listen 80;
    server_name localhost;

    location /api {
      proxy_pass http://salesapp-api:5000;
    }

    location / {
      proxy_pass http://salesapp-client:5173;
    }
  }
}
